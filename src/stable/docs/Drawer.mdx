import { Meta, Title, Subtitle, Description, Primary, Controls, Stories, Story, Source } from '@storybook/blocks';
import * as DrawerStories from '../stories/drawer.stories';

import '../../../.storybook/docs'; // Import all documentation components

<Meta of={DrawerStories} />

# Drawer

<div className="component-summary">
  Drawers slide in from a container to expose additional options and information.
</div>

## Examples

### Basic

<Description of={DrawerStories.Basic} />
<Story of={DrawerStories.Basic} inline={true}/>
<Source of={DrawerStories.Basic} />

### Scrolls

<Description of={DrawerStories.Scroll} />
<Story of={DrawerStories.Scroll} inline={true}/>
<Source of={DrawerStories.Scroll} />

## Slots

<doc-slots-table 
  data={JSON.stringify([
    {"name": "(default)", "description": "The drawer's main content."}, 
    {"name": "label", "description": "The drawer's label. Alternatively, you can use the label attribute."}, 
    {"name": "footer", "description": "The drawer's footer, usually containing action buttons."}
  ])}>
</doc-slots-table>

## JS Properties

<doc-js-properties-table
  data={JSON.stringify([
    {"name": "open", "description": "Indicates whether or not the drawer is open. You can toggle this attribute to show and hide the drawer.", "type": "boolean", "default": "false"}, 
    {"name": "placement", "description": "The direction from which the drawer will open.", "type": "'start' | 'end' | 'top' | 'bottom'", "default": "'end'"}, 
    {"name": "backdrop", "description": "When true, a backdrop will be shown behind the drawer. When set to 'static', the drawer will not close when clicking outside it.", "type": "boolean | 'static'", "default": "true"}, 
    {"name": "scroll", "description": "When true, the body can be scrolled while the drawer is open.", "type": "boolean", "default": "false"}, 
  ])}>
</doc-js-properties-table>

## Events

<doc-events-table 
  data={JSON.stringify([
    {"name": "cod-show", "description": "Emitted when the drawer opens.", "detail": "-"}, 
    {"name": "cod-hide", "description": "Emitted when the drawer closes.", "detail": "-"}
  ])}>
</doc-events-table>

## Methods

<doc-methods-table 
  data={JSON.stringify([
    {"name": "open", "description": "Opens the drawer.", "parameters": "-"}, 
    {"name": "close", "description": "Closes the drawer.", "parameters": "-"}
  ])}>
</doc-methods-table>

## Custom CSS Properties

<doc-css-custom-properties-table 
  data={JSON.stringify([
    {"name": "--cod-drawer-width", "description": "Width of the drawer when placement is start or end.", "defaultValue": "330px"}, 
    {"name": "--cod-drawer-height", "description": "Height of the drawer when placement is top or bottom.", "defaultValue": "330px"}, 
    {"name": "--cod-drawer-header-padding", "description": "Padding of the drawer header.", "defaultValue": "1rem"}, 
    {"name": "--cod-drawer-body-padding", "description": "Padding of the drawer body.", "defaultValue": "1rem"}
  ])}>
</doc-css-custom-properties-table>

## CSS Parts

<doc-css-parts-table 
  data={JSON.stringify([
    {"name": "base", "description": "The component's base wrapper."}, 
    {"name": "backdrop", "description": "The backdrop that covers the screen behind the drawer."}, 
    {"name": "panel", "description": "The drawer's panel where content is rendered."}, 
    {"name": "header", "description": "The drawer's header containing the title and close button."}, 
    {"name": "close-button", "description": "The button that closes the drawer."}, 
    {"name": "body", "description": "The drawer's body."}
  ])}>
</doc-css-parts-table>

## Dependencies

<doc-dependencies-list data={JSON.stringify(["None"])}></doc-dependencies-list>

## Accessibility

Drawer components are designed to be accessible by default:

- The drawer has `role="dialog"` and `aria-modal="true"` 
- When the `label` slot is used, it automatically sets the appropriate `aria-labelledby` attribute
- Focus is trapped inside the drawer when it's open
- The Escape key can be used to close the drawer
- When closed, focus returns to the element that triggered the drawer